# Copy this file to `.env` and replace the placeholder value.
# `.env` を作成したら DISCORD_WEBHOOK_URL を実際の値に書き換えてください。
# Alternatively export DISCORD_WEBHOOK_URL as an environment variable before running the app.
# `.env` を使わない場合は環境変数を直接設定してください。
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/your-webhook-id/your-webhook-token

# Optional: choose the model variants for Stage1 and Stage2
STAGE1_MODEL=lightgbm  # or catboost
STAGE2_MODEL=lstm      # reserved for PyTorch LSTM implementation